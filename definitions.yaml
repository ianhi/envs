base:
  - conda:
    - python
    - jupyterlab
    - scipy
    - numpy
    - matplotlib
    - ipympl
    - pip
    - jupyterlab_vim
    - mpl-interactions
  - pip:
    - black
    - isort
    - jupyterlab-vimrc
    - jupyterlab_code_formatter
    - theme-darcula
xarray:
  - based-on:
    - base
  - conda:
    - xarray
    - dask
    - netCDF4
    - bottleneck
  - pip:
    - tifffile

micro-base:
  - based-on:
      - base
      - xarray
  - pip:
    - napari[all]
    - aicsimageio
    - tifffile
    - zarr

micro-control:
  - based-on:
    - micro-base
  - pip:
    - pymmcore-plus
    - napari-micromanager
    - git+https://github.com/ianhi/pymmcore-MDA-engines
    - pymmcore-MDA-writers

micro-analysis:
  - based-on:
    - micro-base
  - conda:
    - scikit-image
    - scikit-learn
